import Form from "../components/section/Form";

const Contacto = () => {
  return (
    <main>
      <Form />
      <section>
        <div>info</div>
        <div>Mapa</div>
      </section>
    </main>
  );
};

export default Contacto;
